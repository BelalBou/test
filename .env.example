# Variables d'environnement pour le déploiement sur Render
# Copiez ce fichier et ajustez les valeurs selon votre configuration

# Configuration de l'application
NODE_ENV=production
PORT=10000

# Configuration de la base de données PostgreSQL
# Render fournit une URL de base de données complète, mais vous pouvez aussi utiliser des variables séparées
DATABASE_URL=postgresql://username:password@host:port/database
# OU utilisez les variables séparées :
DB_HOST=your-db-host.render.com
DB_PORT=5432
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=your-db-name

# JWT Secret pour l'authentification
JWT_SECRET=your-very-secure-jwt-secret-key-here

# Configuration Stripe (si utilisé)
STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Configuration SendGrid (si utilisé)
SENDGRID_API_KEY=your_sendgrid_api_key
FROM_EMAIL=noreply@yourdomain.com

# Configuration CORS (ajustez selon votre domaine)
CORS_ORIGIN=https://your-app-name.onrender.com

# Autres variables selon vos besoins
API_PREFIX=api
